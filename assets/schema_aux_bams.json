{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/msk/nucleovar/master/assets/schema_input.json",
    "title": "msk/nucleovar pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample_id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample ID must be provided and cannot contain spaces",
                "meta": ["sample_id"]
            },
            "normal_path": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.bam$",
                "errorMessage": "BAM file path for normal samples cannot contain spaces and must have extension '.bam' "
            },
            "duplex_path": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.bam$",
                "errorMessage": "Duplex BAM file path for non-normal samples cannot contain spaces and must have extension '.bam' "
            },
            "simplex_path": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.bam$",
                "errorMessage": "Simplex BAM file path for non-normal samples cannot contain spaces and must have extension '.bam' "
            },
            "type": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Type for the sample must be provided and cannot contain spaces",
                "meta": ["type"]
            }
        },
        "required": []
    }
}
